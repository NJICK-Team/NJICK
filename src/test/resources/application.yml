spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:drillo
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
  data:
    redis:
      port: 6379
      host: localhost

  mail:
    host:
    port:
    username:
